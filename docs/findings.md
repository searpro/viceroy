# Moved to Notion

`docs/findings.md` is no longer the source of truth. All 22 measured findings
(F1–F22) about the local inference stack — audio offsets, sd-api quirks,
image sizing, networking timeouts, Remotion — now live in the **Findings**
database in Notion, one page per finding, filterable by area.

See [`docs/notion.md`](notion.md) for the link.

This file is kept only so a stale link or an old habit doesn't 404 or land on
an empty diff. Don't add content here — it won't be read. Add new findings to
the Notion database instead.
