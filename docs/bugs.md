# Moved to Notion

`docs/bugs.md` is no longer the source of truth. The M5 beta-hardening
tracker now lives in the **Bug Tracker** database in Notion — the
`bug-report` and `fix-bug` skills read and write it directly.

See [`docs/notion.md`](notion.md) for the link.

This file is kept only so a stale link or an old habit doesn't 404 or land on
an empty diff. Don't add content here — it won't be read.
