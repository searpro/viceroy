# Moved to Notion

`docs/PLAN.md` is no longer the source of truth. The build plan — decisions,
stack, data model, pipeline, sd-api integration, risks, working agreement —
now lives in the **Build Plan** page in Notion.

See [`docs/notion.md`](notion.md) for the link and the rest of the Notion
structure (Phases & Milestones, Findings, Architecture Decisions, Bug
Tracker, Progress Tracker).

This file is kept only so a stale link or an old habit doesn't 404 or land on
an empty diff. Don't add content here — it won't be read.
